
import globalInitialState from '../initial-state';

const { entities: initialState } = globalInitialState;

const reducer = (state = initialState, action) => {

	return state;

};

export default reducer;
